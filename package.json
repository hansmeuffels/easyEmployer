{
  "name": "easy-employer",
  "version": "1.0.0",
  "private": true,
  "description": "Eenvoudig werkgevers aanmaken via Loket API",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^0.34.6"
  }
}
